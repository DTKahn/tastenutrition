{
  "name": "tastenutrition-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker proxy/adapter that turns the legacy tastenutrition.com ASP site into a clean JSON API.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test:parse": "node --experimental-strip-types test/parse.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.60.0"
  }
}
